# Find Last Element in an Array (C#)

This simple C# console program prints all elements of an array and then displays the last element.

## 📌 What the Program Does
1. Defines an array of integers.
2. Prints all elements of the array.
3. Prints the last element of the array using the expression `arr[arr.Length - 1]`.
